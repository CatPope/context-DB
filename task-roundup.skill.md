---
name: task-roundup
description: >-
  흩어진 맥락을 수집해 "다음에 무엇을 해야 하는가"를 담당·근거와 함께 정리한다.
  "할 일 정리", "해야 할 일", "지금 뭐 해야 하죠", "다음 할 일", 회의 준비 요청에 사용.
  로컬 작업문서만 보고 답하지 않는다.
---

# Task Roundup — 할 일 정리

수집한 맥락에서 **실행 항목**을 뽑는다. 상태를 서술하는 것은
[`context-briefing`](context-briefing.skill.md) — 이 스킬은 **그 다음 단계**다.

**수집 절차는 [`context-briefing`](context-briefing.skill.md) §1~2를 그대로 따른다**
(대화는 `context-db`, 영속 자료는 `links`로 원본). 여기서는 수집 결과를 **할 일로
바꾸는 부분**만 다룬다.

## 1. 로컬 작업문서만 보고 답하지 않는다

이 스킬이 존재하는 이유다.

> 실제 사례: 로컬 문서 기준으로 **"발표 완료"** 라고 답했으나, 대화를 확인하니
> **일정이 연기되고 그 자리에서 새 방향이 정해져 있었다.** 로컬 문서는 그 시점에
> 고정돼 있어 뒤집힌 사실을 담지 못한다.

**순서가 중요하다.** 대화·문서로 현재 상태를 **먼저** 최신화하고, 그 다음에 로컬
작업문서의 미완 항목을 꺼내 **교정**한다. 반대로 하면 낡은 목록에 최신 사실을
끼워 맞추게 된다.

## 2. 할 일 도출

```bash
# 임박한 일정·회의 — 최신 대화부터
context-db timeline --project <프로젝트> --limit 40 --json
context-db search "회의" --project <프로젝트> --limit 20 --json

# 지시·요청이 누구에게서 왔는지
context-db by-person "<이름>" --limit 20 --json
```

로컬 작업문서에서 `미완`·`진행중` 항목을 뽑되, **위에서 확인한 최신 사실로 교정**한다.

## 3. 산출 형식

**먼저 "수집한 맥락"을 3~6줄로 요약**한다. 어디서 무엇을 확인했는지 근거를 표기한다
(대화 날짜·발화자, 문서명). 그 다음 할 일을 세 갈래로 나눈다.

```
A. 임박 일정 대비   다음 회의·마감 + 안건별 준비 항목
B. 진행 / 종결      끝난 것과 진행 중인 것 (담당 표기)
C. 확인 필요        미확정 — 일정·결정 대기
```

- **담당을 나눈다** — 내가 할 것 / 사용자가 할 것 / 다른 사람이 할 것.
- **로컬 문서와 실제가 어긋나면 실제를 우선**하고, **어긋났다는 사실을 명시**한다.
  조용히 고쳐 쓰면 다음에 같은 혼선이 반복된다.
- 정리 후 로컬 작업문서 갱신이 필요하면 **제안**한다(그 프로젝트의 문서 수정 규칙을 따른다).

## 4. 원칙

- **추측 금지.** 각 할 일은 **출처를 댈 수 있어야 한다** — 어느 대화, 어느 문서, 어느 회의인지.
- **변수는 C로 분리한다.** 일정 변경·부재·승인 대기처럼 확정되지 않은 것은 할 일로 적지
  말고 `확인 필요`에 넣어 사용자에게 결론을 되묻는다.
- 완료로 단정하지 않는다. 근거가 없으면 `확인 필요`다.

## 이력 (필수 기록)
- 생성일: 2026-08-27
- 사용 이력:
  - 2026-08-27 — 프로젝트 전용 스킬을 이 저장소로 옮기며 재작성. 수집 절차는
    `context-briefing` 으로 일원화하고(이전 판은 양쪽이 소스 목록을 각자 들고 있어
    소스가 늘 때마다 두 곳을 고쳐야 했다), 이 스킬에는 할 일 도출만 남겼다.
    대화 수집은 로그 폴더 직접 훑기에서 `context-db` 조회로 바꿨다 — 근거는
    `context-briefing` §1의 실측.

---
name: context-briefing
description: >-
  흩어진 맥락을 종합해 "지금 어디까지 왔는가"를 브리핑한다. context-db로 대화 맥락을
  회수하고, 영속 자료는 링크를 따라 원본을 읽는다. "브리핑", "현황 정리", "지금 어디까지",
  "그동안 무슨 일 있었나" 같은 요청이나, 자리를 비웠다 돌아왔을 때 사용.
---

# Context Briefing — 현황 종합

흩어진 맥락(대화·공유 문서·받은 파일·자료)을 모아 **현재 상태**를 한 장으로 만든다.
할 일 목록을 뽑는 것은 [`task-roundup`](task-roundup.skill.md) — 이 스킬은 **상태 서술**까지다.

조회 명령의 사용법은 [`context-db`](context-db.skill.md) 스킬을 따른다. 여기서는 **무엇을
어느 순서로 모아 어떻게 종합하는가**만 다룬다.

## 1. 수집 원칙 — 어디서 읽을지가 정해져 있다

> **휘발성 원본은 DB에서, 영속 원본은 링크를 따라 직접.** (ADR-017)

| 소스 | 어디서 읽나 | 이유 |
|---|---|---|
| **메신저 대화** | **`context-db`** | 원본이 일정 기간 뒤 사라진다. **DB가 유일본** |
| 공유 문서·받은 파일·자료 | `context-db links` 로 경로를 얻어 **원본 직접** | 원본이 그대로 있으므로 사본을 두지 않는다 |

### ⚠️ 대화 로그 폴더를 직접 훑지 않는다

가장 중요한 함정이다. 메신저 저장 폴더에는 **최근 구간만** 남고 오래된 파일은 사라진다.
DB는 그보다 훨씬 긴 구간을 보존하고 있다.

```
실측 예: DB 보존 115일  vs  원본 폴더 잔존 30일
        전체 항목의 33%가 원본에서 이미 사라져 DB가 유일한 사본
```

**원본 폴더를 직접 읽는 절차는 조용히 과거를 잃는다.** 에러가 나지 않고 결과가 적게
나올 뿐이라 알아채기 어렵다. 대화는 반드시 `context-db`로 조회한다.

## 2. 수집 순서

```bash
# 0) 범위 확인 — 프로젝트명과 소속 소스
context-db projects
context-db sources --json

# 1) 최근 흐름 — 최신순 타임라인
context-db timeline --project <프로젝트> --limit 40 --json

# 2) 주제별 확인 — 회의·일정·결정 등 키워드
context-db search "회의" --project <프로젝트> --limit 20 --json
context-db search "일정" --project <프로젝트> --limit 20 --json

# 3) 영속 자료의 경로 회수 → 원본을 직접 연다
context-db links --type web_doc --json
context-db links --type file --json

# 4) 로컬 작업 문서 — 진행상황·계획서 등 프로젝트가 정한 문서
```

### `--project` 결과가 유난히 적으면 배정 누락을 의심한다

소스가 프로젝트에 배정되지 않으면 필터에서 **조용히 빠진다.** 확인 방법은
`context-db rules` 로 자동 배정 규칙이 무엇을 잡는지 미리 보는 것이다.
누락이 있으면 규칙을 보완하거나 `set-project` 로 재매핑한 뒤 다시 조회한다.

### 대면 회의는 대화에 결정이 남지 않는다

`"회의하러 갈게요"` 류만 있고 결정 내용이 없으면, 그 회의의 결과는 **공유 문서 쪽**에
있다. 대화만 보고 "결정된 것 없음"으로 판단하지 않는다 — `links` 로 문서를 열어 확인한다.

## 3. 산출 형식

- **최근 변화** — 누가·언제·무엇을. 3~7줄
- **현재 상태** — 축별(예: 모델 / 환경 / 인프라 / 문서화) 한 줄씩
- **막힌 것·확인 필요** — 의사결정 대기 항목
- **다음 액션 제안** — 우선순위순, 담당 제안 포함

간결하게, 불릿 위주. 표는 비교가 필요할 때만.

## 4. 원칙

- **추측 최소화** — 소스에 없는 것은 `확인 필요`로 표기한다. 그럴듯하게 메우지 않는다.
- **근거를 댈 수 있게** — 각 항목은 출처(대화 날짜·발화자, 문서명)를 밝힐 수 있어야 한다.
- **상태를 이 스킬에 적지 않는다** — 브리핑 결과는 산출물이지 스킬 내용이 아니다.
  프로젝트의 진행상황 문서에 남기고, 이 스킬은 절차만 유지한다.

> 이 마지막 항목은 실제 사고에서 나왔다. 이전 판에는 스킬 본문에
> `## 현재 상태 스냅샷 (YYYY-MM-DD 기준)` 절이 박혀 있었고, 갱신되지 않은 채 몇 주가
> 지나 **다음 세션이 낡은 사실을 최신인 줄 알고 읽었다.**

## 이력 (필수 기록)
- 생성일: 2026-08-27
- 사용 이력:
  - 2026-08-27 — 프로젝트 전용 스킬을 이 저장소로 옮기며 재작성. 이전 판은 대화 로그
    **폴더를 직접 훑는** 절차였는데, 원본이 30일만 남고 DB가 115일을 보존한다는 실측
    (33%가 유일본)으로 그 절차가 과거를 잃는다는 것이 드러나 `context-db` 조회로 바꿨다.
    스킬 본문이 들고 있던 현재 상태 스냅샷 절은 제거했다.
